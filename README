The goal is to more or less fully document Ruby through tests using both Unit::Test and RSpec, and maybe others, thus giving us a way to test future versions of Ruby against what is currently expected.
